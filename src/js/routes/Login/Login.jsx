import React from 'react';

import ColorPicker from '../../components/ColorPicker';

const Login = (_props) => {
  return (
    <div>
      <ColorPicker />
    </div>
  );
};

Login.propTypes = {};

Login.defaultProps = {};

export default Login;
